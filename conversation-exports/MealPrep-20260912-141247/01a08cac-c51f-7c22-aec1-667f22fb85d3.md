# MealPrep conversation

Task: 01a08cac-c51f-7c22-aec1-667f22fb85d3
Project: C:\Users\laral\Documents\ChatGPT\MealPrep

## user — 09/10/2026 18:56:44

MealPrep/
├── app/
│   ├── \_layout.tsx          # Stack navigator root (sostituisce quello di default)
│   ├── index.tsx            # 01 Lander
│   ├── budget.tsx           # 02 Budget Selection
│   ├── dietary.tsx          # 03 Dietary Needs Selection
│   ├── nutritional.tsx      # 04 Nutritional Goals Selection
│   └── meal-plan.tsx        # 05 Weekly Meal Plan
│
├── components/
│   ├── ui/                  # Button, ProgressBar, Slider, Card... (riusabili)
│   └── meal-plan/           # DayTab, MealCard, IngredientList, RecipeSteps...
│
├── constants/
│   ├── theme.ts             # colori, font, spacing (dal design Figma)
│   └── options.ts           # liste statiche: dietary needs, nutritional goals
│
├── context/
│   └── OnboardingContext.tsx  # stato condiviso tra le schermate: budget, dietaryNeeds[], nutritionalGoals[]
│
├── services/
│   ├── products.ts          # carica/filtra product\_catalog\_en.json
│   └── mealPlanApi.ts       # chiamata LLM per generare il piano settimanale
│
├── types/
│   ├── product.ts           # tipi TS per il catalogo prodotti
│   └── mealPlan.ts          # tipi per il piano pasti generato
│
├── assets/
│   ├── fonts/                # font "Promo" (da stylesheet.css/demo.html)
│   ├── images/
│   └── data/
│       └── product\_catalog\_en.json
│
└── .env                      # OPENAI\_API\_KEY (⚠️ mai committata, va in .gitignore)

crea questa struttura


## assistant — 09/10/2026 18:56:48

Creo lo scheletro Expo/React Native richiesto, con file TypeScript e cartelle pronte per onboarding, catalogo e generazione del piano. Prima verifico il contenuto attuale per non sovrascrivere lavoro esistente.

## assistant — 09/10/2026 19:04:30

La struttura è in scrittura; includo componenti minimi funzionanti, tipi e servizi come base estendibile. Mantengo la chiave API fuori dal controllo versione tramite `.gitignore`.

## assistant — 09/10/2026 19:11:55

Ho completato anche le schermate intermedie e i componenti riusabili. Ora controllo che tutti i percorsi richiesti siano presenti e che non manchino file nella struttura.

## assistant — 09/10/2026 19:12:59

Struttura creata in `C:\Users\laral\Documents\ChatGPT\MealPrep`.

Include:
- schermate Expo Router per l’onboarding e piano pasti;
- componenti UI e meal-plan riusabili;
- tema, opzioni statiche, context condiviso;
- tipi e servizi per catalogo/proposta piano;
- `assets/data/product_catalog_en.json` inizializzato;
- `.env` ignorato da Git e `.env.example` come riferimento.

Nota: `mealPlanApi.ts` usa un endpoint backend `/api/meal-plan`, così la chiave OpenAI non viene inclusa nell’app mobile.
